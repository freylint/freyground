{
  "name": "smi",
  "version": "0.0.1",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "lint": "cargo clippy -- -Dwarnings",
    "build": "wasm-pack build",
    "test": "cargo test",
    "format": "cargo fmt"
  }
}
