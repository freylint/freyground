{
  "name": "smi",
  "version": "0.0.1",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "lint": "cargo clippy -- -Dwarnings",
    "build": "wasm-pack build --out-dir ../../build/cache/lib/smi/",
    "package": "mkdir -p ../../build/dist/lib && cp -r ../../build/cache/lib/smi ../../build/dist/lib/smi",
    "test": "cargo test",
    "format": "cargo fmt"
  }
}
