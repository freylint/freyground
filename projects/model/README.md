# The Model Module
This is a standalone decoupled module containing all of the knowledge
which must be shared between different parts of the program.

## Why Rust?
The choice of rust was primarily made for it's portability
It can without fuss, produce a model which is usable in C/Rust/JS.
The whole point of shared knowledge is that it is shared. Rust
makes this knowledge sharing trivial.